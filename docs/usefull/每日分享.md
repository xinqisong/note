## 2025-12-02 20:33:28

我是谁，我在哪，我干了啥。立flag真是沙雕。

## 2025-06-30 19:04:00
一周一篇小作文总能坚持吧，fuck。刚说一天一篇，第二天就加班，大牛马。然后就忙忘记了，今天查看笔记才发现。
今日依旧是忙碌一天，工作原因有个项目需要用到数据抽取这块东西，然后翻github，找到了个zdh数据平台，支持web配置数据源，发布etl任务，还集成了各种其他的任务方式，目前只需要jdbc抽目标库的方式，其他的就先不研究了。研究着发现所有任务都是放到server端执行的，然而server端需要spark环境，而且原码是scale和java写的，熟悉又陌生。搞不懂。然后就在调度任务那块将发送任务改为本地直接拿数据源调度。目前就这样能用。
还研究一个报表项目，需要原码开源的才能用，最终选择easy-report和ureport其中之一，easy-report实现的简单，可以快速定制开发，最终选择的这个。ureport没有选择是因为一个问题，他的数据源那块没有持久化，唯一值持久化的是后台配置的。也研究了拿原码改的方式，eclipse项目，具体思路是后端新增数据源相关表，字段就是数据源类型、驱动url、用户名、密码、数据库名，然后实现相关web接口。前端ureportjs/src/panel/datasourcePanel.js中新增数据源的时候调用对应接口拿数据源，打开页面调用接口展示数据源。思路很简单，但是没找到后端接口在哪看，后续有时间再看。


**始于2025-06-24 23:45:25**

今天开始记录每日分享，为什么要记录，看到了LinuxDO的一个帖子https://linux.do/t/topic/655040，也有点想法。之前也是想通过某些平台，像语雀，csdn记录的，但是种种原因没做下来。xxx.github.io也是重拾了，保持记录。